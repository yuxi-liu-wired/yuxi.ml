# /etc/nginx/servers.d/localhost.conf
server {
  listen 443 ssl http2;
  listen [::]:443 ssl http2;
  server_name localhost 127.0.0.1 ::1;

  include /etc/nginx/snippets/ssl-localhost.conf;
  root /var/www/yuxi.ml;
  index index.html;

  location / {
    try_files $uri $uri/ =404;
  }
}
